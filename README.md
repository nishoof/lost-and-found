# Lost and Found

Minimal CRUD SPA to demo DevOps concepts. Create items with name + description, edit description, delete when found.

## Local development (Docker)

```bash
docker compose up --build
```

- Frontend: http://localhost:5173
- Backend: http://localhost:8080

## Database

Schema is in `schema.sql`. The MySQL container seeds it automatically.
